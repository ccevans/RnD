<%= simple_form_for @post do |f| %>
	<%= f.input :title %>
	<%= f.input :description %>
	<%= f.input :link %>
	<%= f.input :typeof %>
	<%= f.input :approve %>

	<%= f.button :submit, "Submit", class: "button" %>

<% end %>