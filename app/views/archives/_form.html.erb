<%= simple_form_for @archive, html: {multipart:true} do |f| %>
  <%= f.error_notification %>

  <%= f.input :title %>
  <%= f.input :link %>

	<%= f.button :submit, "Add Blurb", class: "button" %>
	
<% end %>