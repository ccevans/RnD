<%= simple_form_for([@lyric, @lyric.comments.build]) do |f| %>
  <%= f.error_notification %>

  <%= f.input :content, label: "Reply to thread" %>
	<%= f.button :submit, "Comment", class: "button" %>
	
<% end %>