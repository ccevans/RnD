<%= simple_form_for([@lyric, @lyric.comments.build], remote: true) do |f| %>
  <%= f.error_notification %>

  <%= f.input :content, label: "Reply" %>
	<%= f.button :submit, "Comment", class: "button" %>
	
<% end %>